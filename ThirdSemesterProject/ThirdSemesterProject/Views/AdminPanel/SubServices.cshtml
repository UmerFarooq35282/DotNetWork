
@{
    ViewBag.Title = "getSubServices";
    Layout = "~/Views/Shared/_AdminPanelLayout.cshtml";
}

<section class="is-hero-bar">
    <div class="flex flex-col md:flex-row items-center justify-between space-y-6 md:space-y-0">
        <h1 class="title">
            @ViewData["Title"]
        </h1>
    </div>
</section>
<a asp-action="CreateSubService" asp-controller="AdminPanel" style="background-color:#034efc; padding:10px; margin-left:20px; margin-bottom:10px; margin-top:30px; color:aliceblue; border-radius:10px;">
    Create New
</a>
<section class="section main-section">
    <div class="card has-table">
        <header class="card-header">
            <p class="card-header-title">
                <span class="icon"><i class="mdi mdi-account-multiple"></i></span>
                @ViewData["Title"]
            </p>
            <a href="#" class="card-header-icon">
                <span class="icon"><i class="mdi mdi-reload"></i></span>
            </a>
        </header>
        <div class="card-content">
            <table>
                <thead>
                    <tr>
                        <th>Service Name</th>
                        <th>Description</th>
                        <th>Charges/Month</th>
                        <th>Code</th>
                        <th>Main Service</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ViewBag.SbSrvListItem)
                    {
                        <tr>
                            <td data-label="Name">@item.SubServiceName</td>
                            <td data-label="Company">@item.SubServiceDescription</td>
                            <td data-label="City">$@item.SubServiceCharges</td>
                            <td data-label="City">@item.SubServiceCode</td>
                            <td data-label="City">@item.ServiceId</td>
                            <td class="actions-cell">
                                <div class="buttons right nowrap">
                                    <a href="#" class="button small green --jb-modal" data-target="sample-modal-2" type="button">
                                        <span class="icon"><i class="mdi mdi-eye"></i></span>
                                    </a>
                                    <a href="#" class="button small red --jb-modal" data-target="sample-modal">
                                        <span class="icon"><i class="mdi mdi-trash-can"></i></span>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>

