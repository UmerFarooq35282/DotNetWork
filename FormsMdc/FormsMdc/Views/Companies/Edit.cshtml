@model FormsMdc.Models.Companies

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Companies</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CompanyName" />
            <div class="form-group">
                <label asp-for="DistType" class="control-label"></label>
                <select asp-for="DistType" class="form-control"></select>
                <span asp-validation-for="DistType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
